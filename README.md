// install:
    $ npm init -y
    $ npm install express

// this directory requires CORS installation:
    npm install cors
	
// to run:
	$ node PetRESTServer.js
	$ node app.js
	open frontEnd.html
